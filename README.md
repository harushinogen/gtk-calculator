## Simple calculator app implemented using Gtk 4 & Vala

### Building

```
meson build
cd build
ninja
```

It will generate executable `calculator` inside build directory.

You might need to install some dependencies when building the project.
